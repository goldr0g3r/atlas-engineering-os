"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function CreateBomForm({projectId}:{projectId:string}){const router=useRouter();const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);const r=await fetch(`/api/projects/${projectId}/boms`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(d))});const b=await r.json();if(!r.ok)return setError(b.error??"Save failed");router.push(`/projects/${projectId}/boms/${b._id}`);router.refresh()}return <form onSubmit={submit} className="grid gap-3 rounded-2xl bg-atlas-panel p-4"><h2 className="text-xl font-semibold">Create BOM</h2><input name="name" required placeholder="BOM name" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="revision" placeholder="Revision, e.g. A" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><textarea name="description" placeholder="Purpose or build variant" className="rounded-xl bg-atlas-bg px-4 py-3"/>{error&&<p className="text-atlas-red">{error}</p>}<button className="min-h-12 rounded-xl bg-atlas-accent font-semibold text-slate-950">Create BOM</button></form>}
