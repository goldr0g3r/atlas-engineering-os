export function ItemCard({title,subtitle,meta,status}:{title:string;subtitle?:string;meta?:string;status?:string}) {
  return <article className="rounded-3xl border border-white/10 bg-atlas-panel p-5 shadow-lg"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><h3 className="break-words font-semibold text-white">{title}</h3>{subtitle&&<p className="mt-1 break-words text-sm text-slate-400">{subtitle}</p>}{meta&&<p className="mt-3 break-words text-xs text-slate-500">{meta}</p>}</div>{status&&<span className="shrink-0 rounded-full bg-white/10 px-3 py-1 text-xs text-slate-300">{status}</span>}</div></article>;
}
