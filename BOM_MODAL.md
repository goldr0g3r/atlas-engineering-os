# BOM creation interaction

The BOM list creation form is no longer permanently rendered above the list. A New BOM button opens an accessible modal. Successful creation navigates directly to the new BOM. The existing component-entry and supplier-source drawers inside a BOM are unchanged.
