"use client";
import {useState} from "react";import{useRouter}from"next/navigation";
export function CreateProjectForm(){const router=useRouter();const[error,setError]=useState("");const[saving,setSaving]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setError("");const data=new FormData(e.currentTarget);const res=await fetch("/api/projects",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(data))});const body=await res.json();setSaving(false);if(!res.ok){setError(body.error??"Unable to create project");return}router.push(`/projects/${body._id}`);router.refresh()}return <form onSubmit={submit} className="grid gap-3 rounded-2xl bg-atlas-panel p-4"><h2 className="text-xl font-semibold">New project</h2><input name="name" required maxLength={120} placeholder="Project name" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><textarea name="description" maxLength={2000} placeholder="Project goal and scope" className="rounded-xl bg-atlas-bg px-4 py-3"/><div className="grid grid-cols-2 gap-3"><select name="status" className="min-h-12 rounded-xl bg-atlas-bg px-3"><option>planning</option><option>active</option><option>on-hold</option><option>completed</option></select><select name="priority" className="min-h-12 rounded-xl bg-atlas-bg px-3"><option>medium</option><option>low</option><option>high</option><option>critical</option></select></div>{error&&<p className="text-atlas-red">{error}</p>}<button disabled={saving} className="min-h-12 rounded-xl bg-atlas-accent font-semibold text-slate-950">{saving?"Creating...":"Create project"}</button></form>}
