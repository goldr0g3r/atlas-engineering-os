import{auth}from"@/auth";import{redirect}from"next/navigation";
export default async function SettingsPage(){const session=await auth();if(!session?.user)redirect("/");return <main className="mx-auto max-w-5xl space-y-4"><div><h1 className="text-3xl font-bold tracking-tight text-slate-950 dark:text-white">Settings</h1><p className="mt-1 text-slate-500 dark:text-slate-400">Application preferences and account controls.</p></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-[#101b2d] space-y-4"><h2 className="font-semibold">Account</h2><div className="grid gap-3 sm:grid-cols-2"><div><p className="text-sm text-slate-500">Name</p><p className="font-medium">{session.user.name??"—"}</p></div><div><p className="text-sm text-slate-500">Email</p><p className="font-medium">{session.user.email}</p></div></div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-[#101b2d] space-y-4"><h2 className="font-semibold">Sign out</h2><p className="text-sm text-slate-500 dark:text-slate-400">Sign out of your account on this device.</p><form action="/api/auth/signout" method="POST"><button className="min-h-11 rounded-xl bg-red-600 px-5 font-semibold text-white hover:bg-red-700">Sign out</button></form></section></main>}
