import Link from"next/link";import{requireUserId}from"@/lib/auth-guard";import{getCollection}from"@/lib/collections";import{requireProject}from"@/lib/project-access";import{serialize}from"@/lib/object-id";import{CreateBomForm}from"@/components/create-bom-form";import{BomCard}from"@/components/bom-card";export const dynamic="force-dynamic";
export default async function Boms({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const userId=await requireUserId();await requireProject(userId,projectId);const boms:any[]=serialize(await(await getCollection("bomLists")).find({userId,projectId}).sort({updatedAt:-1}).toArray());return <main className="space-y-4"><CreateBomForm projectId={projectId}/><section className="grid gap-3 md:grid-cols-2">{boms.map(b=><BomCard key={b._id} bom={b} projectId={projectId}/>)}</section></main>}
