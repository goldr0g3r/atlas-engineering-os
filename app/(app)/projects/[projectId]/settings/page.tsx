import{requireUserId}from"@/lib/auth-guard";import{requireProject}from"@/lib/project-access";import{serialize}from"@/lib/object-id";import{ProjectSettingsForm}from"@/components/project-settings-form";export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const userId=await requireUserId();const project:any=serialize(await requireProject(userId,projectId));return <main className="space-y-5"><div><h2 className="text-2xl font-semibold">Project settings</h2><p className="text-slate-500">Project-level configuration and lifecycle controls.</p></div><ProjectSettingsForm project={project} projectId={projectId}/></main>}
