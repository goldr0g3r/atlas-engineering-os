import{getCollection}from"@/lib/collections";import{requireUserId}from"@/lib/auth-guard";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const userId=await requireUserId();const items=await(await getCollection("activityEvents")).find({userId,projectId}).sort({createdAt:-1}).limit(100).toArray();return <main><h2 className="text-2xl font-semibold">Activity</h2><div className="mt-4 grid gap-2">{items.length===0?<p className="text-slate-500">No recorded activity yet.</p>:items.map((x:any)=><article key={x._id.toString()} className="rounded-xl border border-slate-200 bg-white p-4 dark:border-white/10 dark:bg-[#101b2d]"><p>{x.message??x.action}</p><time className="text-xs text-slate-500">{x.createdAt}</time></article>)}</div></main>}