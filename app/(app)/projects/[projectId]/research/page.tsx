import{requireUserId}from"@/lib/auth-guard";import{getCollection}from"@/lib/collections";import{requireProject}from"@/lib/project-access";import{serialize}from"@/lib/object-id";import{CreateResourceForm}from"@/components/create-resource-form";import{ResourceItemCard}from"@/components/resource-item-card";export const dynamic="force-dynamic";
const KIND="research" as const;
export default async function Page({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const userId=await requireUserId();await requireProject(userId,projectId);const items:any[]=serialize(await(await getCollection(KIND)).find({userId,projectId}).sort({updatedAt:-1}).toArray());return <main className="space-y-4"><CreateResourceForm projectId={projectId} kind={KIND}/><section className="grid gap-3 md:grid-cols-2">{items.map(i=><ResourceItemCard key={i._id} item={i} projectId={projectId} kind={KIND}/>)}</section></main>}
