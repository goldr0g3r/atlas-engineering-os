import{requireUserId}from"@/lib/auth-guard";import{getCollection}from"@/lib/collections";export const dynamic="force-dynamic";
export default async function Overview({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const userId=await requireUserId();const names=["tasks","bomLists","research","notes","links","experiments"] as const;const counts=await Promise.all(names.map(n=>getCollection(n).then(c=>c.countDocuments({userId,projectId}))));return <main><h2 className="mb-3 text-2xl font-semibold">Overview</h2><div className="grid grid-cols-2 gap-3 lg:grid-cols-3">{names.map((n,i)=><div key={n} className="rounded-2xl bg-atlas-panel p-5"><p className="capitalize text-slate-400">{n}</p><p className="text-3xl font-bold">{counts[i]}</p></div>)}</div></main>}
