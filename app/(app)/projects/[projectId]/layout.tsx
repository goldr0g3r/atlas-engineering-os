import{Suspense}from"react";import{ProjectHeader}from"@/components/project-header";import{ProjectHeaderSkeleton}from"@/components/skeletons";
export default async function Layout({children,params}:{children:React.ReactNode;params:Promise<{projectId:string}>}){const{projectId}=await params;return <div className="space-y-5"><Suspense fallback={<ProjectHeaderSkeleton/>}><ProjectHeader projectId={projectId}/></Suspense>{children}</div>}
