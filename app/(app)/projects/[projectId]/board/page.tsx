import{requireUserId}from"@/lib/auth-guard";import{getCollection}from"@/lib/collections";import{requireProject}from"@/lib/project-access";import{serialize}from"@/lib/object-id";import{KanbanBoard}from"@/components/kanban-board";
export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const userId=await requireUserId();await requireProject(userId,projectId);const items:any[]=serialize(await(await getCollection("workItems")).find({userId,projectId}).sort({position:1}).toArray());return <main className="space-y-4"><div><h2 className="text-2xl font-semibold">Board</h2><p className="text-slate-500">Work items grouped by delivery status.</p></div><KanbanBoard items={items} projectId={projectId}/></main>}
