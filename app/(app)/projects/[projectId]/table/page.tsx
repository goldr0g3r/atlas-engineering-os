import{requireUserId}from"@/lib/auth-guard";import{getCollection}from"@/lib/collections";import{requireProject}from"@/lib/project-access";import{serialize}from"@/lib/object-id";import{ProjectModuleForm}from"@/components/project-module-form";import{WorkItemsTable}from"@/components/work-items-table";
export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const userId=await requireUserId();await requireProject(userId,projectId);const items:any[]=serialize(await(await getCollection("workItems")).find({userId,projectId}).sort({position:1}).toArray());return <main className="space-y-4"><div className="flex items-start justify-between"><div><h2 className="text-2xl font-semibold">Table</h2><p className="text-slate-500">A dense project work-item view.</p></div><ProjectModuleForm projectId={projectId} kind="work-items" label="Work item"/></div><WorkItemsTable items={items} projectId={projectId}/></main>}
