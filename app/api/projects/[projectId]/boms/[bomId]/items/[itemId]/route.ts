import{NextResponse}from"next/server";import{requireUserId}from"@/lib/auth-guard";import{getCollection}from"@/lib/collections";import{ensureProjectResource}from"@/lib/project-access";import{objectId}from"@/lib/object-id";import{nowIso,safeNumber}from"@/lib/utils";import{routeError}from"@/lib/api-response";
export async function PATCH(r:Request,{params}:{params:Promise<{projectId:string;bomId:string;itemId:string}>}){try{const{projectId,bomId,itemId}=await params;const userId=await requireUserId();await ensureProjectResource(userId,projectId,"bomLists",bomId);await ensureProjectResource(userId,projectId,"bomItems",itemId);const b=await r.json();const $set:Record<string,unknown>={updatedAt:nowIso()};if(typeof b.partName==="string"){const n=b.partName.trim();if(!n)return NextResponse.json({error:"partName cannot be empty"},{status:400});$set.partName=n;}if(typeof b.partNumber==="string")$set.partNumber=b.partNumber;if(typeof b.manufacturer==="string")$set.manufacturer=b.manufacturer;if(typeof b.supplier==="string")$set.supplier=b.supplier;if(typeof b.supplierPartNumber==="string")$set.supplierPartNumber=b.supplierPartNumber;if(typeof b.quantity!=="undefined")$set.quantity=Math.max(0,safeNumber(b.quantity,1));if(typeof b.unit==="string")$set.unit=b.unit;if(typeof b.unitCost!=="undefined")$set.unitCost=Math.max(0,safeNumber(b.unitCost,0));if(typeof b.currency==="string")$set.currency=b.currency;if(typeof b.link==="string")$set.link=b.link;if(typeof b.datasheetUrl==="string")$set.datasheetUrl=b.datasheetUrl;if(typeof b.status==="string")$set.status=b.status;if(typeof b.notes==="string")$set.notes=b.notes;if(Object.keys($set).length===1)return NextResponse.json({error:"Nothing to update"},{status:400});await(await getCollection("bomItems")).updateOne({_id:objectId(itemId,"itemId")},{$set});return NextResponse.json({ok:true})}catch(e){return routeError(e)}}
export async function DELETE(_r:Request,{params}:{params:Promise<{projectId:string;bomId:string;itemId:string}>}){try{const{projectId,bomId,itemId}=await params;const userId=await requireUserId();await ensureProjectResource(userId,projectId,"bomLists",bomId);await ensureProjectResource(userId,projectId,"bomItems",itemId);await(await getCollection("bomItems")).deleteOne({_id:objectId(itemId,"itemId")});return NextResponse.json({ok:true})}catch(e){return routeError(e)}}
