# Atlas reviewed UI fix

## Applied fixes
- Replaces the known inline BOM-item form with an Add button and accessible right-side drawer.
- Keeps the BOM table as the primary page surface.
- Groups optional sourcing fields under progressive disclosure.
- Adds a reusable centered form dialog for short create flows.
- Adds a static audit script for inline forms, legacy theme tokens, obsolete global routes, and uninitialized refs.

## Validation boundary
This overlay can only definitively validate against the local repository during installation. The complete current repository was not retrievable through connected enterprise search or the public GitHub URL from this environment.
