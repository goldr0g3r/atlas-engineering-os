"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function CreateBomItemForm({projectId,bomId}:{projectId:string;bomId:string}){const router=useRouter();const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const d=Object.fromEntries(new FormData(form));const r=await fetch(`/api/projects/${projectId}/boms/${bomId}/items`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(d)});const b=await r.json();if(!r.ok)return setError(b.error??"Save failed");form.reset();setError("");router.refresh()}return <form onSubmit={submit} className="grid gap-3 rounded-2xl bg-atlas-panel p-4"><h2 className="text-xl font-semibold">Add BOM item</h2><div className="grid gap-3 md:grid-cols-2"><input name="partName" required placeholder="Part name" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="partNumber" placeholder="Part number" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="manufacturer" placeholder="Manufacturer" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="supplier" placeholder="Supplier" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="quantity" type="number" min="0" step="any" defaultValue="1" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="unit" defaultValue="pcs" placeholder="Unit" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="unitCost" type="number" min="0" step="any" placeholder="Unit cost" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="currency" defaultValue="INR" placeholder="Currency" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="link" type="url" placeholder="Supplier link" className="min-h-12 rounded-xl bg-atlas-bg px-4 md:col-span-2"/></div>{error&&<p className="text-atlas-red">{error}</p>}<button className="min-h-12 rounded-xl bg-atlas-accent font-semibold text-slate-950">Add item</button></form>}
