"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{Plus,X}from"lucide-react";
export function BomItemDrawer({projectId,bomId}:{projectId:string;bomId:string}){const[open,setOpen]=useState(false),[error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const res=await fetch(`/api/projects/${projectId}/boms/${bomId}/items`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const body=await res.json();if(!res.ok)return setError(body.error??"Save failed");form.reset();setOpen(false);setError("");router.refresh()}return <><button onClick={()=>setOpen(true)} className="inline-flex min-h-11 items-center gap-2 rounded-full bg-[#123b3a] px-5 text-sm font-medium text-white"><Plus className="h-4 w-4"/>Add component</button>{open&&<div className="fixed inset-0 z-[80] bg-slate-950/35 backdrop-blur-sm" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><aside className="absolute inset-y-0 right-0 w-full max-w-md overflow-y-auto bg-white p-6 shadow-2xl"><div className="mb-6 flex items-center justify-between"><div><p className="text-sm text-slate-500">BOM component</p><h2 className="text-2xl font-semibold">Add component</h2></div><button onClick={()=>setOpen(false)} className="grid h-11 w-11 place-items-center rounded-full bg-slate-100" aria-label="Close"><X/></button></div><form onSubmit={submit} className="space-y-4">{[["partName","Component name","text",true],["partNumber","Part number","text",false],["manufacturer","Manufacturer","text",false],["supplier","Supplier","text",false],["quantity","Quantity","number",true],["unit","Unit","text",false],["unitCost","Unit cost","number",false],["currency","Currency","text",false],["link","Supplier URL","url",false]].map(([name,label,type,req]:any)=><label key={name} className="block text-sm font-medium">{label}<input name={name} type={type} required={req} step={type==="number"?"any":undefined} defaultValue={name==="quantity"?1:name==="unit"?"pcs":name==="currency"?"INR":undefined} className="atlas-field mt-1 min-h-12 w-full rounded-xl px-4"/></label>)}<label className="block text-sm font-medium">Notes<textarea name="notes" rows={4} className="atlas-field mt-1 w-full rounded-xl p-4"/></label>{error&&<p className="rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<div className="flex gap-3 pt-3"><button type="button" onClick={()=>setOpen(false)} className="min-h-12 flex-1 rounded-xl border border-slate-200">Cancel</button><button className="min-h-12 flex-1 rounded-xl bg-[#123b3a] font-medium text-white">Save component</button></div></form></aside></div>}</>}
