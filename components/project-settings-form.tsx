"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function ProjectSettingsForm({project,projectId}:{project:any;projectId:string}){const router=useRouter();const[saving,setSaving]=useState(false);const[deleting,setDeleting]=useState(false);const[confirmDelete,setConfirmDelete]=useState(false);const[error,setError]=useState("");
const input="min-h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 text-sm outline-none focus:border-teal-500 focus:ring-4 focus:ring-teal-500/10 dark:border-white/10 dark:bg-white/5";
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setError("");const form=e.currentTarget;try{const r=await fetch(`/api/projects/${projectId}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const b=await r.json();if(!r.ok)throw new Error(b.error||"Save failed");router.refresh()}catch(e){setError(e instanceof Error?e.message:"Save failed")}finally{setSaving(false)}}
async function doDelete(){setDeleting(true);try{const r=await fetch(`/api/projects/${projectId}`,{method:"DELETE"});if(!r.ok)throw new Error("Delete failed");router.push("/projects")}catch(e){setError(e instanceof Error?e.message:"Delete failed");setDeleting(false)}}
return<div className="space-y-6"><form onSubmit={submit} className="space-y-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-[#101b2d]"><h2 className="font-semibold">General</h2><label className="block text-sm font-medium">Project name<input name="name" required maxLength={120} defaultValue={project.name} className={input}/></label><label className="block text-sm font-medium">Description<textarea name="description" rows={3} maxLength={2000} defaultValue={project.description??""} className={`${input} py-3`}/></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-medium">Status<select name="status" defaultValue={project.status} className={input}><option value="planning">Planning</option><option value="active">Active</option><option value="on-hold">On hold</option><option value="completed">Completed</option><option value="archived">Archived</option></select></label><label className="block text-sm font-medium">Priority<select name="priority" defaultValue={project.priority} className={input}><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="critical">Critical</option></select></label></div>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={saving} className="min-h-11 rounded-xl bg-teal-600 px-5 font-semibold text-white disabled:opacity-60">{saving?"Saving...":"Save changes"}</button></form><section className="rounded-2xl border border-red-200 bg-white p-5 shadow-sm dark:border-red-500/20 dark:bg-[#101b2d]"><h2 className="font-semibold text-red-700 dark:text-red-400">Danger zone</h2><p className="mt-1 text-sm text-slate-500">Permanently delete this project and all its data.</p>{confirmDelete?<div className="mt-3 flex items-center gap-3"><button onClick={()=>setConfirmDelete(false)} className="min-h-11 rounded-xl border border-slate-200 px-4 dark:border-white/10">Cancel</button><button onClick={doDelete} disabled={deleting} className="min-h-11 rounded-xl bg-red-600 px-5 font-semibold text-white disabled:opacity-60">{deleting?"Deleting...":"Confirm delete"}</button></div>:<button onClick={()=>setConfirmDelete(true)} className="mt-3 min-h-11 rounded-xl border border-red-300 bg-white px-5 font-semibold text-red-600 hover:bg-red-50">Delete project</button>}</section></div>}
