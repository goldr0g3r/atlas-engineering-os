"use client";
import Link from "next/link";import{usePathname}from"next/navigation";import{Boxes,FolderKanban,LayoutDashboard,Settings}from"lucide-react";
const links=[["/projects","Home",LayoutDashboard],["/projects","Projects",FolderKanban],["/projects","BOMs",Boxes],["/settings","Settings",Settings]]as const;
export function BottomNav(){const path=usePathname();return <nav aria-label="Mobile navigation" className="fixed inset-x-0 bottom-0 z-30 grid grid-cols-4 border-t border-slate-200 bg-white/95 px-[max(0.5rem,env(safe-area-inset-left))] pb-[env(safe-area-inset-bottom)] shadow-[0_-8px_30px_rgba(15,23,42,.08)] backdrop-blur lg:hidden dark:border-white/10 dark:bg-[#0b1625]/95">{links.map(([href,label,Icon])=>{const active=path===href||path.startsWith(`${href}/`);return <Link key={href} href={href} aria-current={active?"page":undefined} className={`flex min-h-16 flex-col items-center justify-center gap-1 text-[11px] font-medium transition ${active?"text-teal-700 dark:text-teal-300":"text-slate-500 hover:text-slate-950 dark:text-slate-400 dark:hover:text-white"}`}><Icon className="size-5"/><span>{label}</span></Link>})}</nav>}
