type Segment={label:string;value:number;color:string};
export function DonutChart({segments,centerLabel}:{segments:Segment[];centerLabel:string}){const total=segments.reduce((s,x)=>s+x.value,0);let angle=0;const gradient=segments.map(s=>{const start=angle;angle+=total?s.value/total*360:0;return `${s.color} ${start}deg ${angle}deg`}).join(",");return <div className="flex flex-col items-center gap-5 sm:flex-row"><div className="relative grid h-36 w-36 shrink-0 place-items-center rounded-full" style={{background:total?`conic-gradient(${gradient})`:'#e7eff1'}}><div className="grid h-24 w-24 place-items-center rounded-full bg-white text-center"><div><p className="text-2xl font-semibold">{total}</p><p className="text-xs text-atlas-muted">{centerLabel}</p></div></div></div><div className="w-full space-y-2">{segments.map(s=><div key={s.label} className="flex items-center justify-between text-sm"><span className="flex items-center gap-2"><i className="h-2.5 w-2.5 rounded-full" style={{background:s.color}}/>{s.label}</span><strong>{s.value}</strong></div>)}</div></div>}
export function HorizontalBars({items,max}:{items:{label:string;value:number;color:string}[];max:number}){return <div className="space-y-4">{items.map(i=><div key={i.label}><div className="mb-1 flex justify-between text-sm"><span>{i.label}</span><strong>{i.value}</strong></div><div className="h-2.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full" style={{width:`${max?Math.min(100,i.value/max*100):0}%`,background:i.color}}/></div></div>)}</div>}
export function MoneyBars({budget,actual,committed,formatted}:{budget:number;actual:number;committed:number;formatted:(n:number)=>string}){const max=Math.max(budget,actual+committed,1);return <div className="space-y-4">{[{label:"Approved budget",value:budget,color:"#2c8df5"},{label:"Actual expenses",value:actual,color:"#ff953e"},{label:"Committed",value:committed,color:"#22b8a7"}].map(i=><div key={i.label}><div className="mb-1 flex justify-between gap-3 text-sm"><span>{i.label}</span><strong>{formatted(i.value)}</strong></div><div className="h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full" style={{width:`${Math.min(100,i.value/max*100)}%`,background:i.color}}/></div></div>)}</div>}
