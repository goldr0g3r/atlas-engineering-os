"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function CreateResourceForm({projectId,kind}:{projectId:string;kind:"tasks"|"research"|"notes"|"links"|"experiments"}){const router=useRouter();const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const r=await fetch(`/api/projects/${projectId}/${kind}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const b=await r.json();if(!r.ok)return setError(b.error??"Save failed");form.reset();setError("");router.refresh()}return <form onSubmit={submit} className="grid gap-3 rounded-2xl bg-atlas-panel p-4"><h2 className="text-xl font-semibold">Add {kind}</h2><input name="title" required maxLength={200} placeholder="Title" className="min-h-12 rounded-xl bg-atlas-bg px-4"/>{(kind==="links"||kind==="research")&&<input name="url" type="url" placeholder="URL" className="min-h-12 rounded-xl bg-atlas-bg px-4"/>}<textarea name="description" placeholder="Details / notes" className="rounded-xl bg-atlas-bg px-4 py-3"/>{error&&<p className="text-atlas-red">{error}</p>}<button className="min-h-12 rounded-xl bg-atlas-accent font-semibold text-slate-950">Save</button></form>}
