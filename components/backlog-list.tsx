"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{EditDeleteActions}from"@/components/edit-delete-actions";import{ProjectModuleForm}from"@/components/project-module-form";
const priorityColors:Record<string,string>={critical:"bg-red-500",high:"bg-amber-500",medium:"bg-blue-500",low:"bg-slate-400"};
export function BacklogList({items,projectId}:{items:any[];projectId:string}){const router=useRouter();const[editingId,setEditingId]=useState<string|null>(null);
async function promote(id:string){await fetch(`/api/projects/${projectId}/work-items/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:"ready"})});router.refresh()}
if(editingId)return<ProjectModuleForm projectId={projectId} kind="work-items" label="Work item" item={items.find(i=>i._id===editingId)}/>;
return<div className="space-y-2">{items.map(i=><div key={i._id} className="flex items-start gap-4 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm dark:border-white/10 dark:bg-[#101b2d]"><span className={`mt-1 h-2.5 w-2.5 shrink-0 rounded-full ${priorityColors[i.priority]??"bg-slate-400"}`}/><div className="flex-1 min-w-0"><div className="flex items-center gap-2"><h3 className="font-semibold">{i.title}</h3><span className="rounded bg-slate-100 px-1.5 py-0.5 text-[10px] dark:bg-white/10">{i.type}</span></div>{i.description&&<p className="mt-1 text-sm text-slate-500 line-clamp-1">{i.description}</p>}</div><div className="flex items-center gap-2 shrink-0"><button onClick={()=>promote(i._id)} className="min-h-8 rounded-lg bg-teal-600 px-3 text-xs font-semibold text-white hover:bg-teal-700">Promote</button><EditDeleteActions onEdit={()=>setEditingId(i._id)} onDelete={async()=>{await fetch(`/api/projects/${projectId}/work-items/${i._id}`,{method:"DELETE"});router.refresh()}} label={i.title}/></div></div>)}</div>}
