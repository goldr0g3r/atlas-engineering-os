"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function DomainCreateForm({projectId,kind}:{projectId:string;kind:"risks"|"milestones"|"costs"|"budgets"|"work-items"}){const router=useRouter();const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const response=await fetch(`/api/projects/${projectId}/${kind}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const body=await response.json();if(!response.ok){setError(body.error??"Save failed");return}form.reset();setError("");router.refresh()}return <form onSubmit={submit} className="grid gap-3 rounded-2xl bg-atlas-panel p-4"><h2 className="text-xl font-semibold">Add {kind}</h2><input name="title" required maxLength={200} placeholder="Title" className="min-h-12 rounded-xl bg-atlas-bg px-4"/>{kind==="risks"&&<div className="grid grid-cols-2 gap-3"><input name="probability" type="number" min="1" max="5" defaultValue="1" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="impact" type="number" min="1" max="5" defaultValue="1" className="min-h-12 rounded-xl bg-atlas-bg px-4"/></div>}{kind==="milestones"&&<input name="targetDate" type="date" required className="min-h-12 rounded-xl bg-atlas-bg px-4"/>}{(kind==="costs"||kind==="budgets")&&<div className="grid grid-cols-2 gap-3"><input name="amount" type="number" min="0" step="0.01" required placeholder="Amount" className="min-h-12 rounded-xl bg-atlas-bg px-4"/><input name="currency" defaultValue="INR" maxLength={3} className="min-h-12 rounded-xl bg-atlas-bg px-4"/></div>}<textarea name="description" maxLength={4000} placeholder="Details" className="rounded-xl bg-atlas-bg px-4 py-3"/>{error&&<p role="alert" className="text-atlas-red">{error}</p>}<button className="min-h-12 rounded-xl bg-atlas-accent font-semibold text-slate-950">Save</button></form>}
