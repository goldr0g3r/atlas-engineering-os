# Overview route correction

The global navigation no longer exposes `/dashboard` as Overview because that legacy route redirects to project selection. Project Overview is exclusively `/projects/[projectId]` and is linked from the project navigation. The page now contains live task, risk, financial, milestone, sourcing, and workspace-record visualizations.
