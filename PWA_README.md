# Atlas installable app patch

Adds an App Router web manifest, branded 192/512/maskable icons, a conservative service worker, a supported-browser install button, and iOS Add to Home Screen instructions. Authenticated navigation, APIs, dashboard data, and project data are deliberately not cached. Installability requires HTTPS in production or localhost during development.
