import Link from"next/link";const links=[["","Overview"],["tasks","Tasks"],["boms","BOMs"],["research","Research"],["notes","Notes"],["links","Links & docs"],["experiments","Experiments"]]as const;export function ProjectNav({projectId}:{projectId:string}){return <nav className="scrollbar-none flex gap-2 overflow-x-auto pb-1">{links.map(([path,label])=><Link className="atlas-chip whitespace-nowrap rounded-full px-4 py-2 text-sm transition hover:border-[#123b3a]" key={path} href={`/projects/${projectId}${path?`/${path}`:""}`}>{label}</Link>)}</nav>}
